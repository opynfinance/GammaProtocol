contract('OtokenFactory Tester', accounts => {
  const creatorAddress = accounts[0]

  before('set up contracts', async () => {
    // Do the setup here
  })

  describe('What are you testing', () => {
    it('Specifics of Test', async () => {
      // Leave this blank for now
    })
  })
})
